#include "main.h"
/**
 * add - add two integers
 * Description: a function that adds two integers and returns the result
 * @x: integer 1
 * @y: integer 2
 * Return: (sum)
 */
int add(int x, int y);

{
	int sum;
	sum = x + y;
	return (sum);
}
